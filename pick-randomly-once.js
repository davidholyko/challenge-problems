// given an array of integers
// write a function that picks out a random value and doesn't pick it again
// and outputs an array of the picked values

const input = [1, 2, 3, 4, 5, 6, 7];

const pick = values => {};

const output = pick(input);
