// sort an array of only 0's and 1's
const sort1 = values => {};

const input = [0, 1, 1, 0, 1, 1];

const output = sort1(input);

console.log(output); // should be [0, 0, 1, 1, 1, 1]

// sort an array of only 0's and 1's and 2's
// do not use array iteration methods
// time complexity: O(N)
// space complexity: O(1)

const sort2 = values => {};

const input = [2, 0, 0, 1, 1, 2, 2];

const output = sort2(input);

console.log(output); // should be [0, 0, 1, 1, 2, 2, 2]
