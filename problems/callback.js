// create a function that takes a value and a callback
// and then uses the callback on the value

const myFunction = (value, callback) => {};

const output1 = myFunction(10, (a) => a + 10);

console.log(output1); // logs 20
