// create a function that takes two objects and merges properties on each other

const obj1 = {
  a: 10,
  b: 11,
  c: 12,
  d: 13,
};

const obj2 = {
  a: 20,
  b: 21,
  c: 12,
};

const merge = (obj1, obj2) => {};

const output = merge(obj1, obj2);

console.log(output);
