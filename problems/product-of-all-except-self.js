// write a function that is the product of the rest of the items except self

const productOfAllExceptSelf = (inputArray) => {};

const output = productOfAllExceptSelf([1, 2, 3, 4, 5]);

console.log(output); // [120, 60, 40, 30, 20]

/*
    BONUS: Complete without using division
*/
